{
  "name": "grappleship-mcp",
  "version": "0.1.0",
  "description": "MCP server for GrappleShip — chat-first scene/asset/prefab tooling for s&box.",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.5.0"
  }
}
